image: oberbichler/ubuntu-dev

before_script:
    - conda install numpy

stages:
    - test

test:
    stage: test
    script:
        - echo "Testing"
        - pwd
        - python3 --version
        - python3 -m unittest discover
    tags:
        - test
