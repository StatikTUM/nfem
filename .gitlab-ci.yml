image: oberbichler/ubuntu-dev

stages:
    - test

test:
    stage: test
    script:
        - conda install numpy
        - echo "Testing"
        - pwd
        - python3 --version
        - python3 -m unittest discover
    tags:
        - test
