image: gitlab.lrz.de:5005/gu42dib/test_ci:latest

stages:
    - test

test:
    stage: test
    script:
        - echo "Testing"
        - pwd
        - python3 --version
        - python3 -m unittest discover
    tags:
        - test
